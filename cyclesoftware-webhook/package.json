{
  "name": "cyclesoftware-webhook",
  "description": "CycleSoftware webhook receiver voor project powergrid-whatsapp-bot",
  "private": true,
  "main": "index.js",
  "engines": {
    "node": "24"
  },
  "scripts": {
    "deploy": "firebase deploy --only functions --project powergrid-whatsapp-bot",
    "logs": "firebase functions:log --project powergrid-whatsapp-bot",
    "serve": "firebase emulators:start --only functions --project powergrid-whatsapp-bot"
  },
  "dependencies": {
    "firebase-admin": "^13.6.0",
    "firebase-functions": "^7.0.0"
  }
}
